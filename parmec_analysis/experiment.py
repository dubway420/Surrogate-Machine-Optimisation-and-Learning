class Experiment:

    def __init__(self, name, model, dataset, features, labels):
        self.name = name
        self.model = model
        self.dataset = dataset
        self.features = features
        self.labels = labels
